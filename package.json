{
  "name": "webcaptrue",
  "version": "0.2.0",
  "private": true,
  "description": "Chrome 109-compatible offline web capture extension",
  "scripts": {
    "test": "node scripts/validate.mjs && node scripts/smoke.mjs"
  }
}
